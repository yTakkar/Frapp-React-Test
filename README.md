# Usage
1. First install all dependencies:
    ```bash
    npm install
    ```

2. Then start the app.
    ```bash
    npm start
    ```

3. Open in the browser
    ```javascript
    localhost:8080
    ```

